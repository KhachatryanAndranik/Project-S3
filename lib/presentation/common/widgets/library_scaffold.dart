import 'package:flutter/material.dart';

class LibraryScaffold extends Scaffold {
  const LibraryScaffold({super.key, super.appBar, super.body}) : super(
          backgroundColor: const Color.fromARGB(255, 255, 207, 188),
        );
}